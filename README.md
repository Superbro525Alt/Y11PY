# Royale Seige

## Client

To run the client run:

```bash
python src/client.py --ip {ip} --name {0 or 1}
```

Where the ip is the ip printed out when you run the server (see below). And the name is 0 or 1 depending on the player. 

## Server

To run the server:

```bash
python src/server.py
```
